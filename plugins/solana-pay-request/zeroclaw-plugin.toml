name = "solana-pay-request"
version = "0.1.0"
description = "Build Solana Pay URLs and QR payloads for payment requests"
author = "ZeroClaw Labs"
wasm_path = "solana_pay_request.wasm"
capabilities = ["tool"]
permissions = ["config_read"]
